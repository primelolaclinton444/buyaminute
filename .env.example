# Example database connection string for local development or CI.
# Copy to .env and set to a Postgres connection string (Neon, etc.).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# LiveKit (server-side token minting)
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
